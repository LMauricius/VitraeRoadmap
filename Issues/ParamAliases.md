ParamAliases is a bad name.
It limits the functionality that's semantically relevant.
It's also confusing.

Better name:
- needs to allow 'redirection' of parameters
- NEW NEXT FEATURE: 'build in' values - redirect to a value instead of another param.